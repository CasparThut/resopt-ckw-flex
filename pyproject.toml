[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "hems_resopt"
version = "0.1.0"
description = "A package for HEMS resource optimization."
authors = [
    { name = "Your Name", email = "your.email@example.com" }
]
readme = "README.md"
#license = { text = "MIT" }

[project.urls]
Homepage = "https://your-homepage-url.com"

[tool.setuptools]
packages = { find = {exclude = ["tests"]}} 
